#include "Mesh.hpp"

#include <iostream>
#include <vector>
#include <exception>
#include <map>
#include <functional>
#include <numeric>

typedef std::vector<std::vector<std::vector<std::pair<int, glm::vec3>>>> norm_data;
